# Cloudflare Preview

Preview da branch `feat/backend-rust-complete` disparado em 2026-08-05 após configurar o Cloudflare Pages com o repositório `hepter-studios/Labstar`, build `npm run build`, saída `dist` e backend Rust em `https://labstar-api-mackson.fly.dev`.
